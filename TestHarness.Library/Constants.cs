namespace TestHarness.Library
{
    public class Constants
    {
        public const int ImageWidth = 64;              // Up/Down-scale for faster processing with minimal quality loss.
        public const int ImageHeight = 64;             // Up/Down-scale for faster processing with minimal quality loss.
    }
}
